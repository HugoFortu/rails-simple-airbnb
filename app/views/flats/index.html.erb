<p id="notice"><%= notice %></p>

<h1>Flats</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Description</th>
      <th>Price per night</th>
      <th>Number of guests</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->

</div>
  <div class="row ">
    <% @flats.each do |flat| %>
      <div class="col-3 card m-4 " style="width: 18rem;">
        <img class="card-img-top" src="..." alt="Card image cap">
        <div class="card-body">
          <h5 class="card-title"><%= flat.name %></h5>
          <h6 class="card-text"><%= flat.address %></h6>
          <p class="card-text"><%= flat.description %></p>
          <span class="card-text"><%= flat.price_per_night %></span>
          <span class="card-text"><%= flat.number_of_guests %></span>
          <p><%= link_to 'Show', flat, class: "btn btn-primary mx-1"%>
          <%= link_to 'Edit', edit_flat_path(flat), class: "btn btn-primary mx-1"%>
          <%= link_to 'Destroy', flat, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-primary mx-1"%></p>
        </div>
      </div>
    <% end %>
  </div>
</table>

<br>

<%= link_to 'New Flat', new_flat_path %>
